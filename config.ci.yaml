# CI test configuration - used to verify the Docker image starts correctly
# This config is intentionally minimal and won't actually process emails

llm_providers:
  - name: ci-test
    api_url: https://api.openai.com/v1/chat/completions
    api_key: sk-not-a-real-key
    default_model: gpt-4o-mini

accounts:
  - name: ci-test
    imap:
      host: localhost
      port: 993
      tls: auto
      auth: basic
      username: test@example.com
      password: not-a-real-password
